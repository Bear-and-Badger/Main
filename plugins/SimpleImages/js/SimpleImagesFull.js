$(function() {    $(".Comment .Message img").each(function() {        ShrinkImage( $(this) );    });		$(".image-colorbox-wrap").colorbox({rel:"colorbox-group",maxHeight:'95%',maxWidth:'95%'});});$(window).resize(function() {    $(".Comment .Message img").each(function() {        ShrinkImage( $(this) );    });});function ShrinkImage(img) {    	if( img.closest('blockquote').size() == 0 ) {		post = img.closest("div.Message");	} else {		post = img.closest('blockquote');	}		img.css({maxWidth:post.width()+"px"});			if( img.width() > 0 ) {			if( img.width() > post.width() ) {				img.css({width:post.width()+"px"});		}	} else {		img.bind('load', function() {			ShrinkImage( $(this) );		});	}		if( img.parent('a').size() === 0 ) {		img.wrap( '<a class="image-colorbox-wrap" href="' + img.attr('src') + '" rel="colorbox-group" />' );	}}